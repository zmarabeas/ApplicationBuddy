# Disable optional dependencies to prevent Rollup native module issues
optional=false
# Use npm's legacy peer deps resolution
legacy-peer-deps=true
# Ensure we don't install optional dependencies that cause issues
fund=false
audit=false
# Force npm to ignore optional dependencies completely
omit=optional
# Disable native addons that cause issues in serverless environments
node-linker=hoisted 